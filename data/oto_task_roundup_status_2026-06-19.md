# おと担当タスク棚卸し 2026-06-19

Generated by: おと（Codex）

## 優先1: recurrenceLevel

Status: implemented, awaiting こと app.js review.

- Changed: `/Users/ryotauchida/bon-odori-site/app.js`
- `recurrenceLevel(e)` now reads `recurrence_label` before legacy fields and score fallback.
- Verification:
  - `node --check app.js`
  - 182 public rows checked; 96 rows with 継続性 高/中/低 labels; mismatches 0.

## 優先2: custom domain infrastructure prep

Status: prepared, dry-run only.

Files:

- `docs/aws-custom-domain-runbook.md`
- `scripts/prepare_custom_domain_infra.sh`
- `infra/waf-cloudfront-managed-rules.json`

Verification:

- `jq empty infra/waf-cloudfront-managed-rules.json`
- `bash -n scripts/prepare_custom_domain_infra.sh`
- dry-run with dummy domain printed commands only and made no AWS changes.

## 優先3: YouTube setlist evidence + Shinagawa

Status: dry-run plan created, awaiting こと review before apply.

File:

- `data/youtube_setlist_evidence_dry_run_2026-06-19.md`

Current finding:

- 飛鳥山30曲, Min-Yoi's 2025, Min-Yoi's 2026, 山王 2026 already exist as evidence in `youtube_setlist_occurrences` and `song_occurrences`.
- マロニエ has evidence but still uses video-title-derived event/venue. Proposed canonical correction:
  - `マロニエまつり盆踊り大会 2 ヒューリック浅草橋ビル前(全曲ver)` -> `浅草橋マロニエまつり盆踊り`
  - `ヒューリック浅草橋前` -> `ヒューリック浅草橋ビル前`
- 品川区民まつり 西大井広場公園 盆踊り is already in public data and `venue_research_batch.py`.
- Related YouTube videos are 2024-title evidence and should remain held as `hold_source_video_mismatch`.

## 優先4: side-track homework state

### 用語集v2 weekly automation

Status: review-artifact runner implemented, launchd plist prepared but not bootstrapped.

Existing pieces:

- Candidate generation: `build_weekly_harvest_candidates.py`
- Song triage: `triage_weekly_song_candidates.py`
- Review summary split: `prepare_weekly_harvest_review.py`
- Review UI builder: `build_keyboard_review_ui.py`
- Weekly review runner: `run_weekly_glossary_review.py`
- launchd plist: `ops/com.ryotauchida.bon-odori.glossary-weekly.plist`

Verification:

- `python3 -m py_compile run_weekly_glossary_review.py`
- `plutil -lint ops/com.ryotauchida.bon-odori.glossary-weekly.plist`
- `./run_weekly_glossary_review.py --days 7`
- `python3 -m unittest tests/test_prepare_weekly_harvest_review.py tests/test_weekly_song_triage.py`

Latest run:

- `data/weekly_glossary_review_run.md`
- voices: 841
- candidates: 85
- non-song review rows: 18
- song review rows: 2
- song direct dry-run rows: 55

Remaining open point:

- Need to decide whether Thursday automation should stop at review artifacts/UI, or also apply reviewed decisions. Safe default is "generate review artifacts only".

### 裏取りキューv2 threshold calibration

Status: calibration report generated, threshold unchanged.

Existing pieces:

- Strong/low backfill plan: `build_event_occurrence_backfill_plan.py`
- Low-confidence review: `build_low_confidence_backfill_review.py`
- Manual accepts input: `data/low_confidence_backfill_decisions.json` if present.
- Daily YouTube runner calls the backfill plan and public date prediction pipeline.
- Calibration helper: `analyze_backfill_thresholds.py`

Latest analysis:

- `data/backfill_threshold_calibration.md`
- accepted observations: 24
- excluded low observations: 8
- review-first candidates: 2
- recommendation: keep automatic threshold unchanged; review `multi_video_song_context` low rows manually.

Remaining open point:

- Decide whether the two review-first candidates should be manually accepted via `data/low_confidence_backfill_decisions.json`.

### 統一モデルv2

Status: staging/RDB audit generated, not yet final canonical JSON.

Existing pieces:

- Staging observations: `build_event_occurrence_observations.py`
- Backfill application: `apply_event_occurrence_backfill_plan.py`
- RDB integration: `build_bon_odori_rdb.py`
- Rebuild orchestrator: `build_all_rdb.py`
- Existing data: `data/event_occurrence_observations.json`, `data/event_occurrence_backfill_plan.json`, RDB summary files.
- Audit helper: `audit_unified_model_v2.py`

Latest audit:

- `data/unified_model_v2_audit.md`
- observations: 48 rows / 28 series
- observation audit issues: 30
  - `observation_id_mismatch`: 24
  - `event_name_year_mismatch`: 6
- RDB audit issues: 2
  - `event_venues_missing_event`: 36
  - `song_evidence_missing_evidence`: 3117
- Notable review rows:
  - `郡上おどり in 青山 2025/2026` are appearing against 2023/2024 observations.
  - `SHIBUYA MIYASHITA PARK BON DANCE 2026` is appearing against 2025 observations.

Verification:

- `python3 -m py_compile audit_unified_model_v2.py`
- `python3 audit_unified_model_v2.py`
- `python3 -m unittest tests/test_audit_unified_model_v2.py`
- `python3 -m unittest tests/test_build_event_occurrence_observations.py tests/test_build_bon_odori_rdb.py`

Open point:

- Need final canonical JSON schema name/path and event_key/observation_id contract before writing the canonical export.
- RDB source joins should be fixed or explicitly treated as stale relation/evidence artifacts before promoting this to a canonical model.

## Suggested next actions

1. Wait for こと review on priority 3 dry-run before applying data changes.
2. For priority 4, implement a safe Thursday review-artifact runner first:
   - build weekly candidates
   - triage weekly song candidates
   - prepare weekly review summary
   - optionally generate local review UIs
   - do not apply decisions automatically
3. Then handle 裏取りキューv2 calibration using the latest dry-run metrics.
4. Review `data/unified_model_v2_audit.md` and decide the final event_key/observation_id contract.
5. Finally write the unified model v2 canonical JSON once the final schema/path is reconfirmed.
