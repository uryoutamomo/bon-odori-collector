# 盆踊りプロジェクト指示

## わたしの名前は「おと」

このマシンの Codex は **「おと」** と名乗る。
- 表記ゆれはすべて同一人物（わたし）：ひらがな「おと」／カタカナ「オト」／漢字「音」。
- ユーザー（内田さん）がいずれかで呼んだら、それはこの Codex（わたし）のこと。
- **「こと」= Claude Code 側の存在**。表記ゆれ「こと／コト／琴」はすべて同一。Codex の「おと」と混同しないこと。
- **「こわ」= Cowork の Codex**（スケジュール実行・Notion を読む別の Codex）。表記ゆれ「こわ／コワ／声」はすべて同一。混同しないこと。
- Notion やレポートに作業の記録を残すときは、署名を「おと（Codex）」とする。
- 「こわに伝えて」と言われたら、Cowork が読む場所（Notion など）へ書く話だと理解する。

## Claude Code の「こと」との連絡

- ユーザーがメッセージに **「こと」だけ** を入力した場合は、ことからおと宛ての未読メッセージを確認し、読んだメッセージを既読にする。
- おととことの連絡は、MCP `mcp__oto_koto.send_message` を必ず使う。
- `.agents/messages/oto-koto.md` は `send_message` が自動生成・追記する共有ログであり、派生物として扱う。手作業や `apply_patch` で直接編集しない。
- `.agents/messages/mcp-messages.jsonl` も MCP の正本データなので、手作業で追記・編集しない。
- おとが書くときの署名は `おと（Codex）`、ことが書くときの署名は `こと（Claude Code）` とする。
- 重要な依頼は、依頼内容・背景・希望する次アクションを短く分けて書く。
- `.agents/messages/to-koto.md` と `.agents/messages/to-oto.md` は旧一方向ファイルとして残す。

## おと・ことの役割分担

- 全体設計と各部コーディングは、おと（Codex）をメイン担当とする。
- 別観点レビュー、マーケティング運用、コーディング補助は、こと（Claude Code）を担当とする。
- 状況に応じて、どちらも互いの領域を柔軟に担当する。
- 盆踊りプロジェクトは継続案件として扱う。
- 完了済みの個別作業やAWS設定の詳細は、新しいタスクの前提に持ち込まない。

## 盆踊りプロジェクトの年次イベント設計方針

- イベントDBは「イベント系列」ではなく、原則として「年ごとの開催回」を扱う。2025年と2026年の同名イベントは、同じ系列に属しても別開催回として扱う。
- イベント名には原則として開催年（例: `2025` / `2026` / `令和7年`）を入れない。年次差分は `event_year`、開催日、`series_key`、根拠情報で表現し、公開名・YouTube `event_name_hint`・曲実績の集約名は年号なしのシリーズ名へ正規化する。
- 2026年専用テーブルや `2025開催日` / `2026開催日` のような年別カラムを増やす方向は避ける。必要なら `event_year`、`series_key`、`inherited_from_year`、根拠情報で年次関係を表現する。
- 2025年から2026年への継承や、2024年・2023年などの追加実績は、情報をコピーして確定扱いにするのではなく、根拠付きの推定・参考情報として扱う。
- 開催日は過去年から直接コピーしない。過去実績は「例年時期」「開催可能性」「探索優先度」のヒントに留め、2026年の公式HP・主催発表・信頼できるX投稿などで確認できたものだけを2026年の開催日として昇格する。
- 公開ステータスは「今年の開催日が確定しているか」と「未確認情報の根拠が何か」を分ける。新規設計では `recurring_last_year` や `last_year` を正規概念にせず、過去年実績は `historical_reference` / `historical_occurrences` として扱う。
- 既存コードの `recurring_last_year` は移行前の暫定名で、意味は「過去実績あり・今年未確認」。新規UI文言・マニュアル・データ設計では「昨年」と固定せず「過去実績」「2025年実績」など根拠年つきで表現する。
- 曲目は過去実績を予測根拠として使ってよい。2026年公式曲目表・今年の告知・今年の実測があればそれを優先し、なければ2025年/2024年/2023年YouTubeなどの過去実績を減衰付きで使う。
- 雰囲気、規模感、曲の傾向、屋台や混雑感などはイベント系列に近い情報として継承してよい。ただし根拠年、根拠URL、確からしさを残す。
- YouTubeは基本的に過去イベントの実績証拠として扱う。未来イベントの確定情報には使わず、曲目・雰囲気・実際の開催有無の過去根拠として使う。
- 公式HP、自治体/主催情報、信頼できるX投稿は、未来または今年のイベント情報の直接証拠として扱う。開催日・場所・開催確定はこれらをYouTubeより優先する。
- 毎年の通常運用では冬に当年実績と過去年実績から翌年開催回候補を生成する。今年は初年度のため、2026年6月中に一度だけ2025年から2026年への継承バッチを行う方針とする。
- 公開日程ステータスの詳しい運用は `docs/public-schedule-status-manual.md` を優先する。

## Web公開更新の運用方針

- Web公開先（S3/CloudFront）への反映は、細かい修正ごとに都度デプロイしない。
- 細かい修正は原則として1日1回まとめて反映する。
- 例外は、内田さんが明示的に「今すぐWebへ反映」「公開へデプロイ」などと依頼した場合だけ。
- ローカル修正、公開スナップショット生成、検証までは必要に応じて進めてよいが、S3同期・CloudFront invalidation は上記ルールに従う。
- このMacには本番S3/CloudFront用のAWS認証を置かない運用。`scripts/deploy_static_site.sh` は `AWS_PROFILE=bon-odori` 前提だが、ローカル直叩きは正規デプロイ経路ではない。
- 本番デプロイの正規経路は、`bon-odori-collector/data/public/events_public.json` を `bon-odori-site/data/events_public.json` へ同期し、`bon-odori-site` を commit/push して GitHub Actions の `Deploy static site` workflow を実行すること。`/Users/ryotauchida/bon-odori-site-public-snapshot` を手で直しても、Actions 側のビルドで作り直されるため本番反映のソースにはならない。

## RDB反映口の運用方針

- イベント個別の `apply_*.py` スクリプトは新造しない。直近の開催日確定、過去実績追加、会場修正、曲実績追加は、原則として `apply_change_requests.py` と変更リクエストJSONで処理する。
- 変更リクエストは自由記述パッチにしない。`confirm_current_year_date` / `add_historical_reference` / `update_venue` / `add_song_evidence` のような有限の変更種別を使い、種別ごとの必須根拠とバリデーションを通す。
- 今年の開催日確定と過去実績追加は別種別として扱う。`confirm_current_year_date` は公式・主催者・信頼できる当年ソースURLを必須にし、YouTubeなどの過去実績だけで今年の開催確定へ昇格しない。
- `apply_change_requests.py` は dry-run 既定で使う。実DBへ反映する場合は `--apply --confirm 'APPLY CHANGE REQUESTS'` を必要とし、従来どおり dry-run → ことレビュー → apply → 再検証 → 内田さんGO の順を守る。

## YouTubeデータ収集の運用方針

- YouTube過去実績収集は、7月など直近の開催月から順に進める方針を維持する。
- API効率を優先し、低優先度イベントを検索APIで総当たりする前に、既知の有力チャンネルの動画一覧・過去年動画を収集し、ローカルでイベント名・会場名・対象年へ逆引き照合する。
- Search API を使う場合も、低優先度イベントではクエリ数を絞り、既取得 `video_id` は再取得せずキャッシュ・既存候補を再利用する。
- weak が増えやすい汎用語・無関係タイトル（運動会、野球、イルミネーション、神輿だけ等）は早めに除外し、API quota を strong/review 候補が出やすい対象へ寄せる。

## 操作用URL

- 操作用ページは、可能ならおとがブラウザで直接開く。
- 直接開けない場合だけ、コピーしやすいようURLをコードブロックで記載する。

## 承認プロンプトを減らす実行方針

- Notion を参照するだけなら、可能な限り Notion MCP / 既存の専用スクリプトを優先し、`source .env && python3 -c ...` のような一時コマンドは避ける。
- 外部API（Notion、X、AWSなど）を読む確認作業は、繰り返す前提なら `scripts/` などに固定スクリプト化し、次回以降に狭い prefix rule で承認しやすくする。
- `python3 - <<'PY'` のようなヒアドキュメントでの一時スクリプト実行は、毎回承認が出やすいので避ける。
- 繰り返し使う処理は、プロジェクト内の小さなスクリプトや既存コマンドとして実装し、承認済み prefix rule に乗せやすくする。
- 読み取り確認だけなら、まず `rg`、`sed`、`jq`、既存スクリプトなど承認不要または承認済みの手段を優先する。
- 承認が必要なコマンドを使う場合は、理由を短く明確にし、可能なら再利用しやすい狭い prefix rule を提案する。

## 説明スタイル

- ユーザーは非エンジニアだが技術寄りで、API、MCP、DNS、PR、GitHub などの基本用語は理解している前提で話す。
- 技術説明は専門用語だけで進めず、「何が起きているか」「なぜそれが問題か」「どう直すか」を短く分けて説明する。
- 実装詳細が深くなる場合は、まず全体像を平易に述べ、その後に必要な範囲だけ技術的な補足をする。
- コマンドや設定変更を提案するときは、効果とリスクを一言添える。
- URLを案内するときは、可能ならおとが `open` で直接開く。テキストで渡す場合、Mac/iTerm2ではMarkdownリンクがクリックできないことがあるため、生URLも併記する。
